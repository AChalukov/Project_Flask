pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                ls
                aws sts get-caller-identity
                
                '''            
            }
        }
    }
}
